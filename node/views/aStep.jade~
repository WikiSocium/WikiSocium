div(class="step isInvisible", id="step_#{step_i}")
  div(class="stepTitle")
    #{step.caption}
  br
  #widgets
<<<<<<< HEAD
    script(type = 'text/javascript')
      var step#{step.id}FieldsList = [];
      var notEmptyStep = false;
    - each aWidget in step.widgets
      -var wValue = stepData[aWidget.name]
      !=partial('aWidget', {aWidget:aWidget, stepId:step.id, wValue:wValue})
    script(type = 'text/javascript')
      if(notEmptyStep)
      {
        YUI().use('inputex-string', 'inputex-form', 'inputex-datepicker', 'inputex-timeinterval', 'inputex-group', function(Y){
          groups.push(new Y.inputEx.Group({parentEl: 'step_#{step.id}', fields: step#{step.id}FieldsList}) );
        });
      }
=======
    - var step_id = (step.alias != undefined || step.alias != null) ? step.alias : step_i
    - each widget,widget_i in step.widgets
      !=partial(widget.widget_type, {widget:widget, widget_i:widget_i, step_id:step_id})
>>>>>>> remotes/origin/mermakov_stuff
