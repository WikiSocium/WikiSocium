div(class="baseWidget", id="step_#{step_id}_widget_#{widget.id}")
    - if (widget.InfoText!=undefined)
        !=partial("InfoText", {InfoText:widget.InfoText})
    - if (widget.label==undefined)
        - widget.label=" "
    block content
